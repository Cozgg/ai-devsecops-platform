**Level 1: System Context Diagram**

```mermaid

C4Context
  title C4 Level 1 - System Context Diagram for AI DevSecOps Platform
  
  Person(user, "Developer / User", "Người dùng upload source code, xem kết quả phân tích và báo cáo AI")
  
  System(platform, "AI DevSecOps Platform", "Nền tảng hỗ trợ phân tích mã nguồn, phát hiện rủi ro và sinh báo cáo bằng AI Agent")
  
  System_Ext(scannerTools, "Scanner Tools", "Semgrep, Trivy hoặc npm audit dùng để phân tích source code và dependency")
  System_Ext(llmApi, "AI Model API", "Mô hình AI dùng để giải thích findings và gợi ý khắc phục")
  System_Ext(securityDocs, "Security Knowledge Sources", "OWASP, CWE, CVE, tài liệu scanner và runbook nội bộ")
  
  Rel(user, platform, "Sử dụng hệ thống qua web browser")
  Rel(platform, scannerTools, "Gọi scanner để phân tích mã nguồn")
  Rel(platform, llmApi, "Gửi findings và context để sinh báo cáo AI")
  Rel(platform, securityDocs, "Sử dụng tài liệu làm knowledge base cho RAG")

```
**Level 2: Container Diagram**

```mermaid

C4Container
title C4 Level 2 - Container Diagram for AI DevSecOps Platform

Person(user, "Developer / Student", "Người dùng hệ thống")

System_Boundary(platform, "AI DevSecOps Platform") {
    Container(frontend, "React Frontend", "ReactJS", "Giao diện quản lý project, upload source code, xem scan result và AI report")
    Container(api, "Backend API", "Django REST Framework", "Xử lý API, authentication, project, upload, scan job, findings và report")
    Container(worker, "Celery Worker", "Celery", "Xử lý tác vụ nền như giải nén source, chạy scanner, normalize result và gọi AI Agent")
    ContainerDb(database, "Main Database", "PostgreSQL", "Lưu user, project, source upload, scan job, findings, AI report, knowledge base, log và incident")
    ContainerDb(redis, "Message Broker", "Redis", "Queue trung gian giữa Backend API và Celery Worker")
    Container(storage, "File Storage", "Local Storage", "Lưu file source code upload và source đã giải nén")
    Container(aiLayer, "AI Agent Layer", "LangChain + RAG", "Truy xuất knowledge base, tạo prompt và sinh báo cáo phân tích")
    Container(scanner, "Scanner Runtime", "Semgrep / Trivy / npm audit", "Phân tích source code và dependency")
}

System_Ext(llmApi, "AI Model API", "API của mô hình AI")
System_Ext(securityDocs, "Security Knowledge Sources", "OWASP, CWE, CVE, scanner docs")

Rel(user, frontend, "Truy cập")
Rel(frontend, api, "Gọi REST API", "HTTPS/JSON")
Rel(api, database, "Đọc/ghi dữ liệu")
Rel(api, storage, "Lưu source upload")
Rel(api, redis, "Đẩy scan job vào queue")
Rel(worker, redis, "Nhận scan job từ queue")
Rel(worker, storage, "Đọc source code đã upload")
Rel(worker, scanner, "Chạy scanner")
Rel(worker, database, "Lưu scan status, findings và report")
Rel(worker, aiLayer, "Gửi findings để phân tích")
Rel(aiLayer, database, "Truy xuất knowledge base")
Rel(aiLayer, securityDocs, "Tham chiếu tài liệu bảo mật")
Rel(aiLayer, llmApi, "Gọi AI model để sinh báo cáo")

```
**Level 3: Component Diagram**

```mermaid

C4Component
title C4 Level 3 - Component Diagram for Backend API and Worker

Container_Boundary(api, "Django REST API") {
    Component(authModule, "Authentication Module", "Django Auth / DRF", "Đăng ký, đăng nhập, xác thực người dùng")
    Component(projectModule, "Project Module", "DRF ViewSet", "Quản lý project mã nguồn")
    Component(uploadModule, "Source Upload Module", "DRF API", "Nhận file source code, validate và lưu metadata")
    Component(scanJobModule, "Scan Job Module", "DRF API", "Tạo scan job, theo dõi trạng thái scan")
    Component(findingModule, "Finding Module", "DRF API", "Cung cấp API xem danh sách findings")
    Component(reportModule, "AI Report Module", "DRF API", "Cung cấp API xem báo cáo AI")
    Component(logModule, "Log & Incident Module", "DRF API", "Quản lý log hệ thống và incident")
}

Container_Boundary(worker, "Celery Worker") {
    Component(scanTask, "Scan Task", "Celery Task", "Điều phối quá trình xử lý scan job")
    Component(extractService, "Source Extract Service", "Python Service", "Giải nén và chuẩn bị source code")
    Component(scannerRunner, "Scanner Runner", "Python Service", "Gọi Semgrep, Trivy hoặc npm audit")
    Component(normalizer, "Finding Normalizer", "Python Service", "Chuẩn hóa kết quả scanner về format chung")
    Component(riskScoring, "Risk Scoring Engine", "Python Service", "Tính security score và risk level")
    Component(aiCodeAgent, "AI Code Analysis Agent", "LangChain + RAG", "Phân tích findings và sinh gợi ý khắc phục")
    Component(logAnalyzer, "Log Analyzer", "Python Service", "Phân tích log và phát hiện bất thường cơ bản")
    Component(aiDevOpsAgent, "AI DevOps Agent", "LangChain", "Phân tích incident và đề xuất hướng xử lý")
}

ContainerDb(database, "PostgreSQL", "Database", "Lưu dữ liệu hệ thống")
ContainerDb(redis, "Redis", "Message Broker", "Lưu queue scan job")
Container(storage, "File Storage", "Local Storage", "Lưu source code upload")
System_Ext(scannerTools, "Scanner Tools", "Semgrep / Trivy / npm audit")
System_Ext(llmApi, "AI Model API", "Mô hình AI")

Rel(authModule, database, "Đọc/ghi user")
Rel(projectModule, database, "Đọc/ghi project")
Rel(uploadModule, storage, "Lưu file source")
Rel(uploadModule, database, "Lưu metadata source upload")
Rel(scanJobModule, database, "Tạo và cập nhật scan job")
Rel(scanJobModule, redis, "Đẩy scan job vào queue")

Rel(scanTask, redis, "Nhận scan job")
Rel(scanTask, database, "Cập nhật trạng thái scan job")
Rel(scanTask, extractService, "Gọi bước giải nén")
Rel(extractService, storage, "Đọc source upload")
Rel(scanTask, scannerRunner, "Gọi scanner")
Rel(scannerRunner, scannerTools, "Chạy công cụ scanner")
Rel(scannerRunner, normalizer, "Gửi raw result")
Rel(normalizer, database, "Lưu normalized findings")
Rel(normalizer, riskScoring, "Gửi findings đã chuẩn hóa")
Rel(riskScoring, database, "Cập nhật security score")
Rel(scanTask, aiCodeAgent, "Yêu cầu sinh AI report")
Rel(aiCodeAgent, database, "Đọc findings và knowledge base")
Rel(aiCodeAgent, llmApi, "Gọi AI model")
Rel(aiCodeAgent, database, "Lưu AI report")

Rel(logAnalyzer, database, "Đọc log và tạo incident")
Rel(aiDevOpsAgent, database, "Đọc incident/log")
Rel(aiDevOpsAgent, llmApi, "Gọi AI model")
Rel(aiDevOpsAgent, database, "Lưu incident AI report")
```
